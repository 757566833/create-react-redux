// import { SearchViewRef } from '../../view/SearchView/SearchView_cmpt';
// import model from '../../model/model';
export default class Control {
    // static async demo() {
    //     const result = await model();
    //     if (typeof result !== 'number') {
    //            let data = eval('(' + result + ')');
    //           if (data.code == 0) {
    //                SearchViewRef.setfunc(data.data);
    //            } else {
    //                SearchViewRef.interfaceError(data.msg);
    //            }
    //        } else {
    //            SearchViewRef.networkError(result);
    //        }
    // }
}