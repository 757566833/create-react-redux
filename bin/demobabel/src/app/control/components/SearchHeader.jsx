// import { SearchHeaderRef } from '../../components/SearchHeader/SearchHeader_cmpt';
// import model from '../../model/model';
export default class Control {
    // static async demo() {
    //     const result = await model();
    //     if (typeof result !== 'number') {
    //            let data = eval('(' + result + ')');
    //           if (data.code == 0) {
    //                SearchHeaderRef.setfunc(data.data);
    //            } else {
    //                SearchHeaderRef.interfaceError(data.msg);
    //            }
    //        } else {
    //            SearchHeaderRef.networkError(result);
    //        }
    // }
}