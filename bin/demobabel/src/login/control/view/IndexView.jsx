// import { IndexViewRef } from '../../view/IndexView/IndexView_cmpt';
// import model from '../../model/model';
export default class Control {
    // static async demo() {
    //     const result = await model();
    //     if (typeof result !== 'number') {
    //            let data = eval('(' + result + ')');
    //           if (data.code == 0) {
    //                IndexViewRef.setfunc(data.data);
    //            } else {
    //                IndexViewRef.interfaceError(data.msg);
    //            }
    //        } else {
    //            IndexViewRef.networkError(result);
    //        }
    // }
}